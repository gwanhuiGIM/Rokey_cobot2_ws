# cobot2_ws — M0609 + RG2 + RealSense D435i
# 범위: 로봇 bringup / 카메라 / 캘리브 / MoveIt(회피경로)까지.
# 음성·LLM·grasp(corecode/VoiceProcessing, GraspSelection)은 여기 없다 — 별도 격리.
#
# ⚠️ 대부분은 pip가 아니라 apt다. 아래 apt 블록을 먼저 실행하고,
#    이 파일은 `pip3 install -r requirements.txt`로 pip 항목만 깐다.
#
# ---------------------------------------------------------------------------
# apt (시스템 파이썬 · rclpy와 함께 쓰려면 반드시 apt여야 한다)
# ---------------------------------------------------------------------------
#   sudo apt install \
#     libpoco-dev \
#     python3-opencv python3-scipy python3-numpy \
#     ros-humble-xacro \
#     ros-humble-joint-state-publisher-gui \
#     ros-humble-realsense2-camera ros-humble-realsense2-description \
#     ros-humble-ros2-control ros-humble-ros2-controllers \
#     ros-humble-hardware-interface \
#     ros-humble-moveit-configs-utils \
#     ros-humble-moveit-simple-controller-manager \
#     ros-humble-moveit-ros-visualization \
#     ros-humble-moveit-ros-perception
#
# 이 랩탑(kimkh, 2026-08-04 dpkg 실측): ros-humble-ros2-control(메타패키지)만 MISSING.
#   단 알맹이인 controller-manager / hardware-interface / ros2-controllers는 설치돼 있어
#   현재 동작에는 지장 없다. 새 머신이면 메타패키지로 한 번에 까는 게 편하다.
# pip로 opencv-python / numpy>=2 를 깔지 말 것 — rclpy·cv_bridge가 깨진다.
#
# ---------------------------------------------------------------------------
# pip (apt에 없는 것만)
# ---------------------------------------------------------------------------

# OnRobot RG2 Modbus TCP 제어 (onrobot-ros2 드라이버, corecode/.../onrobot.py)
# 현재 이 랩탑에 3.6.9 설치됨. upstream README는 3.3.2를 명시하나 3.6.9로 동작 중.
pymodbus>=3.3.2,<4
