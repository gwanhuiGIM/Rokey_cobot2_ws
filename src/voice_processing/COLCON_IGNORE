2026-08-08 — 빌드에서 제외한다. 소스는 지우지 않는다.

이유: setup.py:16 이 `resource/.env`(gitignore 된 API 키 파일)를 data_files 에 강제해서
이 PC 에서는 `colcon build` 가 실패한다. `.env` 는 원래 있어야 하는 파일이지 setup.py 의
버그가 아니므로 setup.py 를 고치지 않는다.

지금 이 패키지를 쓰지 않는다(pick_fsm 은 voice:=false 로 띄운다). 추후 VLA 노드와 통합할 때
되살린다 — 그때 `resource/.env` 를 채우고 이 파일을 지우면 된다.
